# 路由策略

> AGENTS.md 的补充。复合需求分发、配方匹配、粒度闸门、沉淀标准。

---

## 一、架构模型

```
公共底盘（所有路径共享）
  pitfalls / G1-G12 / api-conventions / dict-conventions / 硬约束
  ↓
需求 → Lane 匹配（见 AGENTS.md §一）
  │
  ├── 命中 Lane → 执行管道
  │     ↓
  │   管道执行完毕 → 扫配方匹配表 → 命中则执行配方
  │     ↓
  │   pnpm verify
  │
  └── 不命中 Lane → 扫配方匹配表
        ├── 命中配方 → 执行配方 → pnpm verify（不经过非标 Lane）
        └── 不命中配方 → 非标 Lane → PRD → 确认 → 执行 → verify
              │
              └── 成功后自检 → 可能沉淀为新配方
```

配方是 Lane 和非标 Lane 之间的**缓冲层**——命中了就不走非标 Lane 的重流程。
非标 Lane 保留作为**安全网**——全新场景兜底，同时也是新配方的**输入源**。

---

## 二、完整路由流程

### 2.1 Lane 命中判定

Lane 匹配逻辑不变（AGENTS.md §一）。**"命中"的定义**：

- 需求中任一关键词匹配 Lane 触发词表 → 命中（即使需求也包含其他内容）
- 例："新建用户管理 + 权限控制" → 命中 CRUD Lane（"用户管理"命中），同时也扫配方表
- 需求完全不包含任何 Lane 触发词 → 不命中 Lane

### 2.2 路由顺序

```
1. Lane 匹配（AGENTS.md §一）
   ├── 命中 → 执行 Lane（模板填空）
   │          ↓
   │      Lane 执行完毕 → 扫配方匹配表（§三）
   │          ├── 命中 → 读取配方文件 → 按指令执行 → pnpm verify
   │          └── 不命中 → pnpm verify
   │
   └── 不命中 Lane → 扫配方匹配表（§三）
         ├── 命中配方 → 读取配方文件 → 按指令执行 → pnpm verify
         └── 不命中配方 → 非标 Lane → PRD → 确认 → 执行 → verify
               │
               └── 成功后三问自检 → 可能沉淀为新配方
```

### 2.3 配方执行失败处理

配方指令执行失败（文件冲突、语法错误等）→ 撤销配方改动，回退到 Lane 执行完毕时的状态 → 告知用户失败原因 → 配方不标记为完成，人工判断是否修复配方文件。

### 2.4 示例

例 1：

```
需求: "新建用户管理模块 + 权限控制"
  → Lane 匹配: "用户管理" + "CRUD" → CRUD Lane
  → CRUD Lane 执行完毕
  → 扫配方表: "权限控制" → 命中 rbac-scaffold
  → 执行 rbac-scaffold → pnpm verify
```

例 2：

```
需求: "给项目加上 RBAC 权限控制"
  → Lane 匹配: 不命中任何 Lane
  → 扫配方表: "权限控制" → 命中 rbac-scaffold
  → 执行 rbac-scaffold → pnpm verify（跳过非标 Lane）
```

例 3：

```
需求: "做一个多级审批流页面"
  → Lane 匹配: 不命中任何 Lane
  → 扫配方表: 不命中
  → 非标 Lane → PRD → 执行 → 成功后判断是否沉淀为配方
```

---

## 三、配方匹配表

Lane 执行完毕后，或 Lane 不命中时，**两个时机都扫此表**。
需求文本命中了「关键词」列的任一关键词，则读取并执行对应配方文件。
多行命中则按表顺序依次执行。配方文件自我完整，不依赖 Lane 执行上下文。

### 3.1 匹配规则

- 需求包含关键词列中任一关键词 → 命中
- 同一关键词命中多行 → 全部执行，按表顺序
- 不同配方含重叠关键词 → 允许。表顺序决定执行顺序

### 3.2 关键词冲突处理

当两个配方共享同一关键词时（如"权限"同时出现在配方 A 和配方 B 中），以表顺序为准：先执行的配方不阻塞后执行的配方。如需互斥（只走一个），在触发条件中使用更精确的关键词区分。

### 3.3 匹配表

<!-- 配方匹配表 — 新配方按格式在下方追加一行 -->

| 关键词 | 配方文件 | 说明                                                                 |
| ------ | -------- | -------------------------------------------------------------------- |
| —      | —        | 暂无配方实例。AI 遇到横切场景直接处理，成功后按 §五 标准判断是否沉淀 |

未命中任何关键词 → 跳过，直接进入 pnpm verify。

---

## 四、粒度闸门

对原全局禁止列表中的目录（`src/router/`, `src/layouts/`, `src/stores/`），
配方执行时采用粒度判断，替代全有或全无禁止：

| 改动量                                                  | 处理             |
| ------------------------------------------------------- | ---------------- |
| ≤5 行，纯增量（加一项配置、加一行导入、加一个数组元素） | 直接改，告知用户 |
| 5-30 行，改逻辑但不动函数签名                           | 告知 + 改        |
| >30 行，或改函数签名、删除已有逻辑、重构                | 暂停确认         |

`src/plugins/` 保持全禁止——基础设施变更需用户确认。

---

## 五、配方沉淀标准

AI 在处理未命中配方的横切场景后，逐项自问。

三问全 Yes → 按 `.ai/templates/recipe.md` 格式生成配方文件，
放入 `.ai/recipes/`，并在上方匹配表中注册一行。

1. 这次操作涉及的文件组合，下次还会遇到吗？
2. 操作步骤能写成 3-7 条明确指令吗？
3. 这个场景在未来项目中出现的概率 >50% 吗？

任一 No → 不沉淀。一次性的特殊需求不占坑。

---

## 六、新会话加载顺序

1. `.ai/project-brief.md`（认知底座，AGENTS.md §零 已有）
2. 本文件（路由策略 + 匹配表 + 粒度闸门）
3. AGENTS.md 其余部分（Lane 分发 + 模板 + 闸门）
