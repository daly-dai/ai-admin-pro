# {模块名} — 页面需求

> AI 将零散文档按此模板整理。`[待补充]` 标记表示原始文档中缺失的信息，需要人工补齐。

---

## 基本信息

| 项       | 值       |
| -------- | -------- |
| 模块名   | {module} |
| 实体名   | {Entity} |
| 页面名称 | {中文名} |
| 路由     | /{route} |

---

## 接口文档

### {接口用途，如：列表查询}

```
{GET/POST} {路径}
Content-Type: application/json
```

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
|      |      |      |      |

返回示例：

```json
{
  "resultCode": "0",
  "data": {}
}
```

> 如果接口数量超过 5 个，只列核心的增删改查，其余标注"详见 {原始文档名}"。

---

## 字段说明

> 从所有接口的返回 + 请求中汇总，去重。如果某个字段只出现在特定接口，标注出来。

| 字段 | 类型 | 标签 | 在哪用         | 备注 |
| ---- | ---- | ---- | -------------- | ---- |
|      |      |      | 列表/表单/详情 |      |

---

## 枚举 / 字典

> 如果一个字段有固定值范围，在这里列全。标注来源：前端写死 / 字典接口 / 后端枚举文档。

| 字段 | 值  | 含义 | 来源 |
| ---- | --- | ---- | ---- |
|      |     |      |      |

---

## 页面交互

### 列表页

- 筛选项：{列出可搜索的字段}
- 每页默认条数：{20}
- 操作按钮：{新增 / 批量删除 / 导出 / 无}
- 行操作：{编辑 / 删除 / 查看详情 / 启用停用 / 无}

### 新增/编辑

- 方式：{弹窗 Modal / 独立页 / 抽屉 Drawer}
- 新增和编辑：{共用 / 拆分}
- 编辑时不可修改的字段：{如：机构代码}
- 字段联动：{如：选择 A 类型后出现 B 字段 / 无}

### 详情

- 是否展示：{有 / 无}
- 方式：{弹窗 / 独立页 / 抽屉}
- 字段分组：{如：基本信息 / 联系方式 / 无}

### 特殊交互

- {如：删除需要二次确认}
- {如：切换 Tab 时刷新数据}
- {如：表格行内编辑}
- {无则删掉本节}

---

## 业务规则

- 校验：{如：手机号格式校验 / 机构代码唯一 / 无}
- 状态流转：{如：停用后不可编辑删除 / 无}
- 权限：{如：仅管理员可删除 / 无}
- 默认值：{如：新建后状态默认启用 / 无}
