# mail-template v2 — Task 拆解（模板 CRUD + 报表工作台 + 发布/预览/发送 Demo）

> 需求 PRD：`docs/mail-template/prd.md`（v2，Q1–Q26 已收敛）。
> 场景：模板 CRUD → CRUD Lane；工作台 + mock 在线编辑 + 预览/发送链路属非标部分；实施时遵循 AGENTS.md §零 检查清单、输出锁与两层闸门。
> 执行状态：**Task 0 已完成 ✅，Task 1–7 待实施**。旧 v1 记录见 `specs/mail-template-tasks.md`（历史，勿覆盖）。

## 执行节奏（每 Task 统一）

```
1. TDD seam 确认 —— 列出「被测接口（seam）+ 明确不测」清单，与用户确认后才写码
   （tdd 技能铁律：只在预约定 seam 测试；UI 壳一律不测，交给验收走查）
2. 逻辑核 红→绿 —— 竖切：一条用例（先见红）→ 最小实现转绿 → 下一条；
   禁止横切（先批量写完测试再实现 = 测想象中的行为）
3. UI 组装（本 Task 含页面时）
4. 走验收标准（勾选）
5. pnpm verify ≤3 轮（沙箱内用直接 node/npx 跑 tsc/eslint/prettier）+ 复盘三问
```

**测试纪律**：用例与被测模块同目录 `*.test.ts`，显式 `import { describe, it, expect } from 'vitest'`（不开 globals，不动 tsconfig）；零 any；测试文件在 `src/` 内，自动被 tsc/eslint/prettier 闸门兜底。红线：**红测不提交**，每 Task 收尾 `npx vitest run` 必须全绿（沙箱需 threads 池，原因已写入 `vitest.config.ts` 注释）。

## 整体进度

| 阶段             | Task 数 | 完成 | 待实施 |
| ---------------- | ------- | ---- | ------ |
| 测试基建         | 1       | 1    | 0      |
| 服务层           | 1       | 0    | 1      |
| 渲染内核扩展     | 1       | 0    | 1      |
| 模板列表 CRUD    | 1       | 0    | 1      |
| 工作台（详情）   | 1       | 0    | 1      |
| mock 在线编辑    | 1       | 0    | 1      |
| 预览与发送链路   | 1       | 0    | 1      |
| 接线与收尾       | 1       | 0    | 1      |

> 外部依赖已在册：`exceljs`、`@wangeditor/editor(-for-react)`、`vitest`(dev)；UI 规约：页面零 antd Table/Form/Button/Descriptions（STable/SForm/SButton/SDetail），弹层用 createModal/createDrawer（P001），类型零 any，模块内部 `./` 相对导入（单目录迁移契约）。

## Task 列表

### Task 0 — 测试基建（vitest 接线 + 冒烟） ✅

| 项         | 内容                                                                                                                                                                                                                                                                                                   |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **类型**   | 基建（前置）                                                                                                                                                                                                                                                                                           |
| **输出锁** | 根目录 `vitest.config.ts`、`package.json`（仅加 `scripts.test`）、`src/features/mail-template/excel-to-html/parseExcel.test.ts`                                                                                                                                                                       |
| **模板**   | 无（vitest 5 官方配置；skill: vitest）                                                                                                                                                                                                                                                                |
| **做什么** | 配置：`include: src/**/*.test.ts`、`environment: node`、`pool: threads`（沙箱 forks 池 spawn EPERM，注释说明 WHY）；`package.json` 加 `"test": "vitest run"`；冒烟用例锁定 `.debug/` 三夹具解析已知答案（探针实测，独立于实现）：测试工具.xlsx=红底 #C00000 白字/等线 14pt/32×15；标准答案.xlsx=红底白字 11pt/11×3；红底黑字.xlsx=黑字 #000000（v1 颜色映射翻车点） |
| **TDD seam** | 本 Task 无红绿循环（写的是既有内核的回归快照，先绿锁定，非新逻辑）                                                                                                                                                                                                                                     |

**验收标准**：

- [ ] `npx vitest run` 全绿（三夹具冒烟用例）
- [ ] `pnpm verify` 三闸 0 error（冒烟用例在 src/ 内，被 tsc/eslint/prettier 兜住）
- [ ] 用例显式 import、零 any；期望值为已知答案字面量（非实现推导）

### Task 1 — service 层（mock 数据 + 权限过滤） ⬜

| 项         | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **类型**   | CRUD Lane（服务层）                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **输出锁** | `src/features/mail-template/service/` 下新增：`types.ts`（MailTemplate/Report/SendMailPayload 零 any）、**逻辑核纯函数模块**（如 `templateRules.ts`：占位符/邮箱/必填校验；`reportAccess.ts`：canView 过滤与同名冲突判定）与其同目录 `*.test.ts`、localStorage 适配器（`mockStore.ts`）与 service 门面（`mailTemplateService.ts` `reportService.ts` `mailSender.ts`）                                                                                                                  |
| **模板**   | 读 `.ai/templates/` 相关 CRUD 模板；API 命名遵循 `{Entity}{name}By{HTTP}`                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **做什么** | 数据模型零 any；**结构上把纯核函数与 localStorage 适配器分开**（为修改而设计：接真实后端时只替换适配器，逻辑核与测试原样保留）；localStorage store（元数据 + xlsx base64，大小守卫）；模板 CRUD；报表列表（**只返回 canView**）/上传（同名查重返回冲突，由前端确认后带 overwrite 再覆盖）/下载/保存（回存 fileBase64 并刷新 updatedAt）；发送（延迟成功）；Promise 延迟模拟。预置 1 个示例模板（不含报表）。 |
| **TDD seam** | 被测：reportAccess 的 canView 过滤（无权限文件不进列表返回）、同名冲突判定（冲突标识 → overwrite 后覆盖/异名新增）、发送 payload 形状与延迟成功、占位符/邮箱校验（若落此层）；不测：localStorage 适配器薄壳与 store 组装（页面联调走查，必要时再 vi.stubGlobal）                                                                                                                                                                                                                      |

**验收标准**：

- [ ] `npx vitest run` 全绿（本 Task 逻辑核用例先行红→绿）
- [ ] 刷新后数据不丢；canView=false 的报表不出现在报表列表返回中
- [ ] 同名上传先返回冲突标识，带确认后覆盖；异名新增
- [ ] 发送返回成功 + 回显完整 payload；API 命名与类型约束通过 eslint/tsc

### Task 2 — 渲染内核扩展：exceljs 值回写 ⬜

| 项         | 内容                                                                                                                                                                                                                                                                                                                                                   |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **类型**   | 非标（纯函数，TDD 主战场）                                                                                                                                                                                                                                                                                                                             |
| **输出锁** | `src/features/mail-template/excel-to-html/` 新增 `applyCellEdits.ts` + 同目录 `applyCellEdits.test.ts`                                                                                                                                                                                                                                                 |
| **模板**   | 无                                                                                                                                                                                                                                                                                                                                                     |
| **做什么** | 纯函数：原 xlsx buffer + 单元格编辑记录 `{row,col,value}` → exceljs 写回原 workbook 对应单元格 → `writeBuffer()` 导出（样式 100% 保留）；含单元格边界/类型守卫（文本、数值），编辑越界报错。                                                                                                                                                             |
| **TDD seam** | 被测：`applyCellEdits(buffer, edits)` 导出 buffer 再经 parseExcelFile 往返解析——目标单元格值变化且样式快照与原始一致（`.debug/` 三夹具均可作输入）；越界编辑抛可读错误；非法类型值守卫；不测：无（本 Task 无 UI）                                                                                                                                 |

**验收标准**：

- [ ] `npx vitest run` 全绿（红测先于实现：applyCellEdits 未实现时用例红 → 实现转绿）
- [ ] 对 `.debug/` 回归文件（测试工具.xlsx / 红底黑字.xlsx）改值回写后，样式快照与原始解析结果一致（仅目标单元格值变化）
- [ ] 越界编辑抛可读错误；导出 buffer 可被原渲染链路正常解析

### Task 3 — 模板列表页 CRUD ⬜

| 项         | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **类型**   | CRUD Lane（列表页）                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **输出锁** | `src/features/mail-template/index.tsx` `index.module.css`（改造）+ `TemplateFormModal.tsx`（createModal）+ 预设正文数据文件 + **`validation.ts`（校验规则纯函数）+ 同目录 `validation.test.ts`**                                                                                                                                                                                                                                                                                   |
| **模板**   | 读 `.ai/sdesign/components/` S 组件文档与 CRUD 模板                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **做什么** | STable 列表（模板名称/收件人/抄送/报表数/更新时间/操作：编辑、删除、进入工作台）+ 名称搜索；新增/编辑共用弹窗表单（收件人必填多人邮箱校验、抄送可选、预设 2 套正文选择 + wangEditor，保存校验**恰好 1 个** `{{table}}`）；删除确认（级联删报表）；示例模板可见可删。**校验规则抽 `validation.ts` 纯函数**，供表单与测试共用。                                                                                    |
| **TDD seam** | 被测：validation 纯函数——多人邮箱解析与非法邮箱拦截、恰好 1 个 `{{table}}`（0 个 / ≥2 个均拒绝）、收件人必填；不测：STable 页面渲染、wangEditor 初始化、弹窗交互（走验收走查 + 演示口径提示文案核对）                                                                                                                                                                                                            |

**验收标准**：

- [ ] `npx vitest run` 全绿（validation 用例）
- [ ] 新增/编辑/删除/搜索闭环；校验规则（0 个/≥2 个占位符、非法邮箱）分别拦截并给出 PRD 口径提示
- [ ] 页面零 antd Table/Form/Button/Descriptions；表单弹层符合 P001
- [ ] 「进入工作台」跳转 `/mail/template/:id`

### Task 4 — 工作台（详情）页 ⬜

| 项         | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **类型**   | 非标（多区域工作台）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **输出锁** | `src/features/mail-template/workspace/` 新增（`index.tsx` + 样式 + 子组件：基础信息卡 / 报表列表 / 报表平铺 / iframe 容器）；如出现可抽纯逻辑则 `workspaceLogic.ts` + 同目录 `*.test.ts`（条件性）                                                                                                                                                                                                                                                                                                           |
| **模板**   | 无（P001 弹层规约；SDetail 展示基础信息）                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **做什么** | 顶部基础信息卡（只读、可折叠默认展开，正文富文本渲染且 `{{table}}` 文本可见）；报表区：左列表（图标/名称/更新时间、选中高亮、上传按钮、删除、演示权限开关「仅查看」），右区默认**全部报表平铺**文件卡、空态提示上传；点击卡片 → 整区切换 iframe 阅读模式（iframe 工具栏：返回全部/报表名/阅读⇄编辑/发布）；上传同名冲突 → 确认框覆盖；报表列表头部「演示：无查看权限」开关（驱动 service 过滤，报表从列表消失）。 |
| **TDD seam** | 被测：如出现状态推导纯函数（权限开关 → 可上传/可编辑推导、选中态归约、视图切换推导）抽 workspaceLogic 后红→绿；不测（默认）：布局/折叠/平铺-iframe 切换/空态等 UI 行为——**本 Task 以验收走查为主**，seam 清单开工时再与用户确认                                      |

**验收标准**：

- [ ] 如产出逻辑核：`npx vitest run` 全绿
- [ ] 选中报表左侧同步高亮；返回平铺后可再次进入
- [ ] 空态引导 + 上传/同名覆盖确认闭环；权限开关表现符合 PRD 功能五
- [ ] 基础信息卡折叠/展开与只读展示正确；正文占位符可见

### Task 5 — mock 在线编辑平台页（iframe + postMessage） ⬜

| 项         | 内容                                                                                                                            |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------- |
| **类型**   | 非标（跨域模拟）                                                                                                                |
| **输出锁** | `src/features/mail-template/mock-editor/` 新增（页面组件 + 只读渲染 + 可编辑表格 + 权限错误页 + `communication.ts` 通信纯模块 + 同目录 `communication.test.ts`） |
| **模板**   | 无                                                                                                                              |
| **做什么** | 独立路由页（iframe 目标），`?reportId&mode=read\|edit`；按 PRD postMessage 协议与父页通信（init/ready/save-request/saved/error）；阅读模式只读渲染报表表格；编辑模式可编辑表格**仅单元格值**；canEdit=false 且 mode=edit → 「无编辑权限」错误页；save-request → 收集编辑记录 → Task 2 回写导出 → 回传 saved(fileBase64)。postMessage 契约解析/消息校验/编辑记录收集抽 `communication.ts` 纯函数。 |
| **TDD seam** | 被测：communication 纯函数——postMessage 消息解析与校验（非法消息忽略/兜底）、编辑记录收集（含单元格边界与类型守卫：越界/非法值报错）；不测：iframe 页面三态渲染（阅读/可编辑/无编辑权限错误页）→ 走验收走查                                                        |

**验收标准**：

- [ ] `npx vitest run` 全绿（communication 用例）
- [ ] iframe 内三种呈现正确（阅读 / 可编辑 / 无编辑权限错误页）
- [ ] 编辑→保存→回传的 fileBase64 经 service 存回后，阅读模式与预览渲染显示新值且样式未变
- [ ] postMessage 协议收发完整；错误路径有兜底提示

### Task 6 — 发布 + 预览结合 + 发送链路 ⬜

| 项         | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **类型**   | 非标（链路组装）                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **输出锁** | `src/features/mail-template/preview/`（改造现有 MailPreviewDrawer + 新建发送确认/预览入口 + **`payload.ts` 组装纯函数 + 同目录 `payload.test.ts`**）                                                                                                                                                                                                                                                                                 |
| **模板**   | 无（P001：createDrawer）                                                                                                                                                                                                                                                                                                                                                                                                            |
| **做什么** | 发布按钮（编辑模式工具条）：save-request → 收到 saved → service 存回报表 → toast；页面顶部「预览结合效果」常驻按钮 + 报表下拉切换（仅可查看）；预览抽屉：顶部只读展示主题/收件人/抄送，信纸 = 模板正文 + 所选报表表格注入（复用 excel→html 保真链路）可微调；发送：确认框（收件人数 + 附件名）→ mock 发送 → toast + console payload → 关闭抽屉。无保存/下载/复制动作。**payload 组装（to/cc/subject/bodyHtml/attachments）与 `{{table}}` 替换注入抽 `payload.ts` 纯函数。** |
| **TDD seam** | 被测：payload 组装形状（to/cc/subject/bodyHtml/attachments 完整性与转义）、`{{table}}` 注入替换（所选报表表格 HTML 替换占位符、无占位符时的兜底）、收件人数/附件名统计（确认框数据源）；不测：预览抽屉 UI、确认框交互、下拉切换 → 走验收走查                                                                                                                                                                                  |

**验收标准**：

- [ ] `npx vitest run` 全绿（payload 用例）
- [ ] 发布后存储与预览均为最新内容；发布前无文件/无编辑态时按钮状态正确
- [ ] 预览下拉仅列可查看报表；`{{table}}` 在信纸中被所选报表表格替换且保真
- [ ] 发送确认 → 成功 toast + console 完整 payload；抽屉关闭后模板/报表数据未变

### Task 7 — 路由菜单接线 + README 更新 + 收尾 ⬜

| 项         | 内容                                                                                                                                                                                                                                          |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **类型**   | 收尾                                                                                                                                                                                                                                          |
| **输出锁** | `src/router/routes/index.tsx`、`src/layouts/MainLayout.tsx`（如菜单文案需改）、`src/features/mail-template/README.md`                                                                                                                         |
| **模板**   | 无                                                                                                                                                                                                                                            |
| **做什么** | 路由：列表 `/mail/template`、工作台 `/mail/template/:id`、mock 编辑器内部路由；删除 v1 单页 Demo 遗留组件与逻辑；README 补充 v2 页面/数据模型/service 替换点与**测试命令（`pnpm test` / 沙箱 threads 池说明）**；`.debug/` 回归文件用于手工演示；全量 verify + build + 全链路 UAT 走查（演示彩排：含权限三拦截）。 |

**验收标准**：

- [ ] 三个路由可达；菜单「邮件工具 → 邮件模板」指向列表页
- [ ] v1 遗留单页组件与不再使用的导出/下载逻辑清理干净（不破坏渲染内核复用文件）
- [ ] `npx vitest run` 全量绿；`pnpm verify` 三闸 0 error；`pnpm build` 通过
- [ ] 按 PRD 主链路走查一遍（含权限三种拦截演示）＝ UAT 彩排
