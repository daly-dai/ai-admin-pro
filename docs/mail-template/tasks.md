# mail-template v2 — Task 拆解（模板 CRUD + 报表工作台 + 发布/预览/发送 Demo）

> 需求 PRD：`docs/mail-template/prd.md`（v2，Q1–Q26 已收敛）。
> 场景：模板 CRUD → CRUD Lane；工作台 + mock 在线编辑 + 预览/发送链路属非标部分；实施时遵循 AGENTS.md §零 检查清单、输出锁与两层闸门，每 Task 完成后跑 `pnpm verify`（≤3 轮）+ 复盘三问再进入下一个。
> 执行状态：**全部待实施**。旧 v1 记录见 `specs/mail-template-tasks.md`（历史，勿覆盖）。

## 整体进度

| 阶段           | Task 数 | 完成 | 待实施 |
| -------------- | ------- | ---- | ------ |
| 服务层         | 1       | 0    | 1      |
| 渲染内核扩展   | 1       | 0    | 1      |
| 模板列表 CRUD  | 1       | 0    | 1      |
| 工作台（详情） | 1       | 0    | 1      |
| mock 在线编辑  | 1       | 0    | 1      |
| 预览与发送链路 | 1       | 0    | 1      |
| 接线与收尾     | 1       | 0    | 1      |

> 外部依赖已在册：`exceljs`、`@wangeditor/editor(-for-react)`；UI 规约：页面零 antd Table/Form/Button/Descriptions（STable/SForm/SButton/SDetail），弹层用 createModal/createDrawer（P001），类型零 any，模块内部 `./` 相对导入（单目录迁移契约）。

## Task 列表

### Task 1 — service 层（mock 数据 + 权限过滤） ⬜

| 项         | 内容                                                                                                                                                                                                                                                                                                                                 |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **类型**   | CRUD Lane（服务层）                                                                                                                                                                                                                                                                                                                  |
| **输出锁** | `src/features/mail-template/service/` 下新增（types / store / 模板 / 报表 / 发送），如 `service/types.ts` `service/mockStore.ts` `service/mailTemplateService.ts` `service/reportService.ts` `service/mailSender.ts`                                                                                                                 |
| **模板**   | 读 `.ai/templates/` 相关 CRUD 模板；API 命名遵循 `{Entity}{name}By{HTTP}`                                                                                                                                                                                                                                                            |
| **做什么** | 数据模型（MailTemplate/Report/SendMailPayload 零 any）；localStorage store（元数据 + xlsx base64，大小守卫）；模板 CRUD；报表列表（**只返回 canView**）/上传（同名查重返回冲突，由前端确认后带 overwrite 再覆盖）/下载/保存（回存 fileBase64 并刷新 updatedAt）；发送（延迟成功）；Promise 延迟模拟。预置 1 个示例模板（不含报表）。 |

**验收标准**：

- [ ] 刷新后数据不丢；canView=false 的报表不出现在报表列表返回中
- [ ] 同名上传先返回冲突标识，带确认后覆盖；异名新增
- [ ] 发送返回成功 + 回显完整 payload；API 命名与类型约束通过 eslint/tsc

### Task 2 — 渲染内核扩展：exceljs 值回写 ⬜

| 项         | 内容                                                                                                                                                                                         |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **类型**   | 非标                                                                                                                                                                                         |
| **输出锁** | `src/features/mail-template/excel-to-html/` 新增写回模块（如 `applyCellEdits.ts`）                                                                                                           |
| **模板**   | 无                                                                                                                                                                                           |
| **做什么** | 纯函数：原 xlsx buffer + 单元格编辑记录 `{row,col,value}` → exceljs 写回原 workbook 对应单元格 → `writeBuffer()` 导出（样式 100% 保留）；含单元格边界/类型守卫（文本、数值），编辑越界报错。 |

**验收标准**：

- [ ] 对 `.debug/` 回归文件（测试表格.xlsx / 红底黑字.xlsx）改值回写后，样式快照与原始解析结果一致（仅目标单元格值变化）
- [ ] 越界编辑抛可读错误；导出 buffer 可被原渲染链路正常解析

### Task 3 — 模板列表页 CRUD ⬜

| 项         | 内容                                                                                                                                                                                                                                                                 |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **类型**   | CRUD Lane（列表页）                                                                                                                                                                                                                                                  |
| **输出锁** | `src/features/mail-template/index.tsx` `index.module.css`（改造）+ 新增 `TemplateFormModal.tsx`（createModal）+ 预设正文数据文件                                                                                                                                     |
| **模板**   | 读 `.ai/sdesign/components/` S 组件文档与 CRUD 模板                                                                                                                                                                                                                  |
| **做什么** | STable 列表（模板名称/收件人/抄送/报表数/更新时间/操作：编辑、删除、进入工作台）+ 名称搜索；新增/编辑共用弹窗表单（收件人必填多人邮箱校验、抄送可选、预设 2 套正文选择 + wangEditor，保存校验**恰好 1 个** `{{table}}`）；删除确认（级联删报表）；示例模板可见可删。 |

**验收标准**：

- [ ] 新增/编辑/删除/搜索闭环；校验规则（0 个/≥2 个占位符、非法邮箱）分别拦截并给出 PRD 口径提示
- [ ] 页面零 antd Table/Form/Button/Descriptions；表单弹层符合 P001
- [ ] 「进入工作台」跳转 `/mail/template/:id`

### Task 4 — 工作台（详情）页 ⬜

| 项         | 内容                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **类型**   | 非标（多区域工作台）                                                                                                                                                                                                                                                                                                                                                                                              |
| **输出锁** | `src/features/mail-template/workspace/` 新增（`index.tsx` + 样式 + 子组件：基础信息卡 / 报表列表 / 报表平铺 / iframe 容器）                                                                                                                                                                                                                                                                                       |
| **模板**   | 无（P001 弹层规约；SDetail 展示基础信息）                                                                                                                                                                                                                                                                                                                                                                         |
| **做什么** | 顶部基础信息卡（只读、可折叠默认展开，正文富文本渲染且 `{{table}}` 文本可见）；报表区：左列表（图标/名称/更新时间、选中高亮、上传按钮、删除、演示权限开关「仅查看」），右区默认**全部报表平铺**文件卡、空态提示上传；点击卡片 → 整区切换 iframe 阅读模式（iframe 工具栏：返回全部/报表名/阅读⇄编辑/发布）；上传同名冲突 → 确认框覆盖；报表列表头部「演示：无查看权限」开关（驱动 service 过滤，报表从列表消失）。 |

**验收标准**：

- [ ] 选中报表左侧同步高亮；返回平铺后可再次进入
- [ ] 空态引导 + 上传/同名覆盖确认闭环；权限开关表现符合 PRD 功能五
- [ ] 基础信息卡折叠/展开与只读展示正确；正文占位符可见

### Task 5 — mock 在线编辑平台页（iframe + postMessage） ⬜

| 项         | 内容                                                                                                       |
| ---------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **类型**   | 非标（跨域模拟）                                                                                           |
| **输出锁** | `src/features/mail-template/mock-editor/` 新增（页面组件 + 只读渲染 + 可编辑表格 + 权限错误页 + 通信模块） |
| **模板**   | 无                                                                                                         |
| **做什么** | 独立路由页（iframe 目标），`?reportId&mode=read                                                            | edit`；按 PRD postMessage 协议与父页通信（init/ready/save-request/saved/error）；阅读模式只读渲染报表表格；编辑模式可编辑表格**仅单元格值**；canEdit=false 且 mode=edit → 「无编辑权限」错误页；save-request → 收集编辑记录 → Task 2 回写导出 → 回传 saved(fileBase64)。 |

**验收标准**：

- [ ] iframe 内三种呈现正确（阅读 / 可编辑 / 无编辑权限错误页）
- [ ] 编辑→保存→回传的 fileBase64 经 service 存回后，阅读模式与预览渲染显示新值且样式未变
- [ ] postMessage 协议收发完整；错误路径有兜底提示

### Task 6 — 发布 + 预览结合 + 发送链路 ⬜

| 项         | 内容                                                                                                                                                                                                                                                                                                                                                                   |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **类型**   | 非标（链路组装）                                                                                                                                                                                                                                                                                                                                                       |
| **输出锁** | `src/features/mail-template/preview/`（改造现有 MailPreviewDrawer + 新建发送确认/预览入口）                                                                                                                                                                                                                                                                            |
| **模板**   | 无（P001：createDrawer）                                                                                                                                                                                                                                                                                                                                               |
| **做什么** | 发布按钮（编辑模式工具条）：save-request → 收到 saved → service 存回报表 → toast；页面顶部「预览结合效果」常驻按钮 + 报表下拉切换（仅可查看）；预览抽屉：顶部只读展示主题/收件人/抄送，信纸 = 模板正文 + 所选报表表格注入（复用 excel→html 保真链路）可微调；发送：确认框（收件人数 + 附件名）→ mock 发送 → toast + console payload → 关闭抽屉。无保存/下载/复制动作。 |

**验收标准**：

- [ ] 发布后存储与预览均为最新内容；发布前无文件/无编辑态时按钮状态正确
- [ ] 预览下拉仅列可查看报表；`{{table}}` 在信纸中被所选报表表格替换且保真
- [ ] 发送确认 → 成功 toast + console 完整 payload；抽屉关闭后模板/报表数据未变

### Task 7 — 路由菜单接线 + README 更新 + 收尾 ⬜

| 项         | 内容                                                                                                                                                                                                                    |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **类型**   | 收尾                                                                                                                                                                                                                    |
| **输出锁** | `src/router/routes/index.tsx`、`src/layouts/MainLayout.tsx`（如菜单文案需改）、`src/features/mail-template/README.md`                                                                                                   |
| **模板**   | 无                                                                                                                                                                                                                      |
| **做什么** | 路由：列表 `/mail/template`、工作台 `/mail/template/:id`、mock 编辑器内部路由；删除 v1 单页 Demo 遗留组件与逻辑；README 补充 v2 页面/数据模型/service 替换点说明；`.debug/` 回归文件用于手工演示；全量 verify + build。 |

**验收标准**：

- [ ] 三个路由可达；菜单「邮件工具 → 邮件模板」指向列表页
- [ ] v1 遗留单页组件与不再使用的导出/下载逻辑清理干净（不破坏渲染内核复用文件）
- [ ] `pnpm verify` 三闸 0 error；`pnpm build` 通过；按 PRD 主链路走查一遍（含权限三种拦截演示）
